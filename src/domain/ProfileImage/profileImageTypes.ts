import {Image} from 'react-native-image-crop-picker';

export interface ProfileImageType {
  id: number;
  formData: FormData;
}
export interface ProfileImageServiceType {
  id: number;
  response: Image;
}
